{% extends 'base.html.twig' %}

{% block slider %}
    <div class="container">
        <div id="myCarousel" class="carousel slide carousel-fade" data-interval="4000" data-ride="carousel" data-pause="null">
            <!-- Indicators
            <ol class="carousel-indicators">
                <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
                <li data-target="#myCarousel" data-slide-to="1"></li>
                <li data-target="#myCarousel" data-slide-to="2"></li>
            </ol>    -->
            <!-- Wrapper for slides -->
            <div class="carousel-inner">
                <div class="item active">
                    <img src="{{ asset(category.image) }}" alt="slider1" style="width:100%;"> {# Ici j'ai mis un width pas a 100% ( il le faudrai normalement ) car les images sont trop petites et mal dimensionner  #}
                    <div class="carousel-caption">
                    </div>
                </div>

                <div class="item">
                    <img src="{{ asset(category.image2) }}" alt="slide2" style="width:100%;">{# Ici j'ai mis un width pas a 100% ( il le faudrai normalement ) car les images sont trop petites et mal dimensionner  #}
                    <div class="carousel-caption">
                    </div>
                </div>

                <div class="item">
                    <img src="{{ asset(category.image3) }}" alt="slide3" style="width:100%;">{# Ici j'ai mis un width pas a 100% ( il le faudrai normalement ) car les images sont trop petites et mal dimensionner  #}
                    <div class="carousel-caption">
                    </div>
                </div>
            </div>
        </div>
    </div>
                    
    
{% include 'incs/search_nav_categ.html.twig' %}

    
{% endblock %}


{% block body %}


    <!-- Page Content -->
    <div class="container">

        <div class="row">
            <div class="col-sm-8 category-pdt">
                <h2 class="mt-4">{{ category.titre }}</h2>
                <p>{{ category.description | raw }}</p>
            
                <div class="col-sm-6 liste-produits">    
                    {% for produit in produits %}
                    <ul class="liste-produits">
                        <li>                          
{#                            <i class="fas fa-angle-double-right"></i>#}
                            <a class="produits-pdf" href="{{ asset(produit.pdf)}}" target="_blank">
                                {{ produit.nom }}
                            </a>                         
                        </li>
                    </ul>
                    {% endfor %}
                </div>
            </div>
            {% for stocks in stock %}   
                <div class="col-sm-4 aside">
                    <a class="btn btn-primary btn-lg" href="{{ path('contact')}}">{{ 'devis' | trans }}</a><br>
                    <a class="btn btn-primary btn-lg" href="{{asset(stocks.getPdfNom) }}" target="_blank">{{ 'stocks' | trans }}</a>
                </div>
            {% endfor %}
        </div>

        <!-- /.row 
        <div class="row">
            <div class="col-sm-4 my-4">
                <div class="card">
                    <img class="card-img-top" src="{{ asset(category.image) }}" alt="">
                    <div class="card-body">
                        <h4 class="card-title">Card title</h4>
                        <p class="card-text">Lorem ipsum dolor sit amet, consectetur adipisicing elit. Sapiente esse necessitatibus neque sequi doloribus.</p>
                    </div>
                    <div class="card-footer">
                        <a href="#" class="btn btn-primary">Find Out More!</a>
                    </div>
                </div>
            </div>
            <div class="col-sm-4 my-4">
                <div class="card">
                    <img class="card-img-top" src="{{ asset(category.image2) }}" alt="">
                    <div class="card-body">
                        <h4 class="card-title">Card title</h4>
                        <p class="card-text">Lorem ipsum dolor sit amet, consectetur adipisicing elit. Sapiente esse necessitatibus neque sequi doloribus totam ut praesentium aut.</p>
                    </div>
                    <div class="card-footer">
                        <a href="#" class="btn btn-primary">Find Out More!</a>
                    </div>
                </div>
            </div>
            <div class="col-sm-4 my-4">
                <div class="card">
                    <img class="card-img-top" src="{{ asset(category.image3) }}" alt="">
                    <div class="card-body">
                        <h4 class="card-title">Card title</h4>
                        <p class="card-text">Lorem ipsum dolor sit amet, consectetur adipisicing elit. Sapiente esse necessitatibus neque.</p>
                    </div>
                    <div class="card-footer">
                        <a href="#" class="btn btn-primary">Find Out More!</a>
                    </div>
                </div>
            </div>

        </div>  -->
        <!-- /.row -->
    </div>
    <!-- /.container -->



{% endblock %}
